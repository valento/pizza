export default (state = 0, action) => {
  // Reducer
  //switch( action.type ) {
    //return new state, NO Mutations!!!
    //case 'action.One':
      return state;
    //or return old state if unknown action!!!
    //default : return state;
  //}
}
